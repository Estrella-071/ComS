
export const appendixCContent = {
  en: `
# Appendix C: Pseudocode

Pseudocode is an English-like representation of the code required for an algorithm. It combines natural language with structured programming constructs.

## C.1 COMPONENTS

An algorithm in pseudocode includes several elements.

### C.1.1 Algorithm header
Names the algorithm, e.g., \`Algorithm: FindingSmallest (list)\`.

### C.1.2 Purpose, conditions, and return
- **Purpose**: A brief description of what the algorithm does.
- **Precondition (Pre)**: Requirements that must be met before the algorithm runs.
- **Postcondition (Post)**: Effects of the algorithm after it has run.
- **Return**: The value that the algorithm returns.

### C.1.3 Statement constructs
Pseudocode uses the three basic structured programming constructs:
- **Sequence**: A series of statements executed in order.
- **Selection**: A conditional statement that chooses between different paths (e.g., if-then-else).
- **Loop**: A block of code that is iterated (e.g., while).
`,
  zh: `
# 附錄 C：偽代碼 (Pseudocode)

偽代碼是一種近似英語的表示法，用於描述演算法所需的程式碼。它將自然語言與結構化程式設計建構相結合。

## C.1 組成部分

偽代碼中的演算法包含幾個元素。

### C.1.1 演算法標頭
為演算法命名，例如：\`演算法：尋找最小值 (列表)\`。

### C.1.2 目的、條件與回傳值
- **目的 (Purpose)**：簡要描述該演算法的功能。
- **前置條件 (Precondition, Pre)**：在演算法執行前必須滿足的要求。
- **後置條件 (Postcondition, Post)**：演算法執行後的效果。
- **回傳 (Return)**：演算法回傳的值。

### C.1.3 陳述式建構
偽代碼使用三種基本的結構化程式設計建構：
- **循序 (Sequence)**：按順序執行的一系列陳述式。
- **選擇 (Selection)**：選擇不同路徑的條件陳述式（例如 if-then-else）。
- **迴圈 (Loop)**：重複執行的程式碼區塊（例如 while）。
`,
};
